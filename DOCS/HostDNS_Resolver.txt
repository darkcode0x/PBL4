┌─────────────────────────────────────────────────────────────────┐
│  VICTIM COMPUTER (Lab Machine)                                  │
│  DNS Config: 192.168.1.100 ← IP của Local Resolver            │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ Query: b.0.5.data.example.com
         v
┌─────────────────────────────────────────────────────────────────┐
│  LOCAL DNS RESOLVER (192.168.1.100)                            │
│  - Bạn kiểm soát hoàn toàn                                     │
│  - KHÔNG log, KHÔNG analyze                                     │
│  - Chỉ forward đến Root/TLD như bình thường                    │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ Recursive lookup (bình thường)
         v
┌─────────────────────────────────────────────────────────────────┐
│  ROOT → TLD → C&C SERVER                                        │
│  (Luồng bình thường, nhưng qua resolver của BẠN)              │
└─────────────────────────────────────────────────────────────────┘