┌─────────────────────────────────────────────────────────────────┐
│  VICTIM COMPUTER (Client - Keylogger đang chạy)                 │
└────────┬────────────────────────────────────────────────────────┘
         │ 
         │ 1. Query: b.0.5.48656C6C6F.example.com có IP gì?
         │    (Đây là data exfiltration query, không phải query thật)
         v
┌─────────────────────────────────────────────────────────────────┐
│  ISP DNS RESOLVER (hoặc 8.8.8.8, 1.1.1.1)                       │
│  - Nhận query từ victim                                         │
│  - Check cache: Không có                                        │
│  - Bắt đầu recursive lookup                                     │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 2. Query: example.com được quản lý bởi ai?
         v
┌─────────────────────────────────────────────────────────────────┐
│  ROOT DNS SERVER (a.root-servers.net)                           │
│  Database: {                                                    │
│    ".com": "a.gtld-servers.net (192.5.6.30)",                  │
│    ".org": "a.afilias-nst.org",                                │
│    ".vn": "a.dns.vn"                                           │
│  }                                                              │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 3. Response: "Tôi không biết example.com,
         │              nhưng .com được quản lý bởi TLD server:
         │              a.gtld-servers.net (192.5.6.30)"
         v
┌─────────────────────────────────────────────────────────────────┐
│  ISP RESOLVER (cache TLD info)                                  │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 4. Query đến TLD: example.com nameserver là gì?
         v
┌─────────────────────────────────────────────────────────────────┐
│  TLD DNS SERVER (.com - Verisign)                               │
│  Database (từ Registrar): {                                     │
│    "example.com": {                                             │
│      "NS": ["ns1.example.com", "ns2.example.com"],            │
│      "GLUE": {                                                  │
│        "ns1.example.com": "203.0.113.10",  ← ATTACKER'S IP!   │
│        "ns2.example.com": "203.0.113.10"                       │
│      }                                                          │
│    }                                                            │
│  }                                                              │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 5. Response: "example.com được quản lý bởi:
         │              ns1.example.com (203.0.113.10) ← GLUE RECORD
         │              ns2.example.com (203.0.113.10)"
         v
┌─────────────────────────────────────────────────────────────────┐
│  ISP RESOLVER                                                    │
│  Cache: "example.com NS = 203.0.113.10"                        │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 6. Query cuối cùng đến Authoritative Server:
         │    b.0.5.48656C6C6F.example.com có IP gì?
         v
┌─────────────────────────────────────────────────────────────────┐
│  ATTACKER'S C&C SERVER (203.0.113.10:53)                       │
│  Chạy main.py - DNS Server giả                                 │
│                                                                 │
│  Xử lý:                                                         │
│  1. Parse query: b.0.5.48656C6C6F.example.com                 │
│     - Type: "b" → Data packet                                  │
│     - Packet: 0                                                 │
│     - Connection ID: 5                                          │
│     - Data (hex): 48656C6C6F                                   │
│  2. Decode hex → ASCII: "Hello"                                │
│  3. Lưu vào log file                                           │
│  4. Generate fake response                                      │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 7. Response: 200.123.123.123 (Status Code 200 = OK)
         │    (Đây KHÔNG phải IP thật, chỉ để báo status)
         v
┌─────────────────────────────────────────────────────────────────┐
│  ISP RESOLVER                                                    │
│  Cache response (TTL từ attacker's server)                      │
└────────┬────────────────────────────────────────────────────────┘
         │
         │ 8. Return fake IP: 200.123.123.123
         v
┌─────────────────────────────────────────────────────────────────┐
│  VICTIM COMPUTER                                                 │
│  Keylogger parse response:                                      │
│    - Octet đầu = 200 → OK                                       │
│    - Tăng packet number lên 1                                   │
│    - Tiếp tục gửi keystroke tiếp theo                           │
└─────────────────────────────────────────────────────────────────┘