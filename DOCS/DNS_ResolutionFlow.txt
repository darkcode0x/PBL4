┌─────────────────┐
│   YOUR COMPUTER │
│  (Client)       │
└────────┬────────┘
         │ 1. Query: www.example.com?
         v
┌─────────────────┐
│  ISP DNS        │
│  RESOLVER       │  ← Máy chủ DNS mà bạn config (8.8.8.8, 1.1.1.1)
└────────┬────────┘
         │ 2. Không có cache → Bắt đầu recursive lookup
         │
         │ 3. Query: www.example.com?
         v
┌─────────────────┐
│   ROOT DNS      │
│   (.)           │  ← 13 root servers (a-m.root-servers.net)
└────────┬────────┘
         │ 4. Response: "Hỏi TLD .com tại a.gtld-servers.net"
         v
┌─────────────────┐
│  TLD DNS        │
│  (.com)         │  ← Verisign quản lý
└────────┬────────┘
         │ 5. Response: "Hỏi ns1.example.com (203.0.113.10)"
         v
┌─────────────────┐
│ AUTHORITATIVE   │
│ DNS SERVER      │  ← Server của domain owner (Attacker's server)
│ ns1.example.com │
└────────┬────────┘
         │ 6. Response: "www.example.com = 93.184.216.34"
         v
┌─────────────────┐
│  ISP RESOLVER   │
└────────┬────────┘
         │ 7. Cache result (TTL 3600s)
         │ 8. Return IP to client
         v
┌─────────────────┐
│  YOUR COMPUTER  │
└─────────────────┘