CLIENT (Victim Machine)
     |
     | 1. Local DNS Cache
     |    └─> Không có → tiếp tục
     |
     | 2. Gửi đến DNS Resolver (ISP hoặc 8.8.8.8)
     ├──> Query: a.1.1.1.example.com A record
     |
     v
ISP DNS RESOLVER
     |
     | 3. Resolver kiểm tra cache
     |    └─> Không có → Recursive lookup
     |
     | 4. Query Root DNS Server (.)
     ├──> "Who handles .com?"
     |
     v
ROOT DNS SERVER
     |
     | 5. Response: "Ask TLD Server for .com"
     └──> 192.5.6.30 (a.gtld-servers.net)
     |
     v
ISP DNS RESOLVER
     |
     | 6. Query TLD Server (.com)
     ├──> "Who handles example.com?"
     |
     v
.COM TLD SERVER
     |
     | 7. Response: "Ask ns1.example.com"
     └──> Trả về: ns1.example.com (203.0.113.10)
     |              ↑
     |              └─ Glue Record (từ registrar)
     v
ISP DNS RESOLVER
     |
     | 8. Query Authoritative Server
     ├──> Gửi đến 203.0.113.10:53
     |    Query: a.1.1.1.example.com A
     |
     v
ATTACKER'S DNS SERVER (main.py)
     |
     | 9. Server xử lý:
     |    - Parse query: thấy prefix "a"
     |    - Tạo Connection ID mới (ví dụ: 5)
     |    - Lưu vào DataParserManager
     |
     | 10. Response với FAKE IP
     └──> 123.123.123.5
          └─ Octet cuối = Connection ID
     |
     v
ISP DNS RESOLVER
     |
     | 11. Cache response (TTL từ server)
     └──> Forward về Client
     |
     v
CLIENT
     |
     | 12. Parse IP response:
     |     123.123.123.5 → Connection ID = 5
     |
     └──> Lưu ID = 5 cho các packet sau